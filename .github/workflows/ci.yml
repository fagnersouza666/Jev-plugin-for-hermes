name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install dev dependencies
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip "setuptools>=83.0.0"
          pip install -e '.[dev]'

      - name: Ruff
        run: |
          . .venv/bin/activate
          python -m ruff check .

      - name: Pytest
        run: |
          . .venv/bin/activate
          python -m pytest -q

  security:
    name: security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install dev dependencies and pip-audit
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install --upgrade pip "setuptools>=83.0.0"
          pip install -e '.[dev]'

      - name: pip-audit (dev extras)
        run: |
          . .venv/bin/activate
          bash scripts/pip-audit.sh

      - name: Install gitleaks
        run: |
          mkdir -p "$RUNNER_TEMP/gitleaks"
          curl -fSL "https://github.com/gitleaks/gitleaks/releases/download/v8.28.0/gitleaks_8.28.0_linux_x64.tar.gz" | tar -xz -C "$RUNNER_TEMP/gitleaks"
          echo "$RUNNER_TEMP/gitleaks" >> "$GITHUB_PATH"

      - name: gitleaks
        run: gitleaks detect --source . --verbose --redact
